# GENERATED FILE -- do not hand-edit; scripts/pin_fbcode_snapshot.py rewrites
# the whole file (body from upstream, plus the rev), so local edits are lost.
# Re-pin: ./scripts/pin_fbcode_snapshot.py --write <tag>
# Snapshot: v2026.07.27.00 (upstream weekly release tag)
[manifest]
name = fizz-python
fbsource_path = fbcode/fizz
shipit_project = fizz
shipit_fbcode_builder = true

[git]
repo_url = https://github.com/facebookincubator/fizz.git
rev = ddb1b60503999c763fd31b18eb0d95faa62c6e3b

[build]
builder = cmake
subdir = fizz

[build.not(os=linux)]
builder = nop

[cmake.defines]
BUILD_EXAMPLES = OFF

[cmake.defines.os=linux]
CMAKE_POSITION_INDEPENDENT_CODE = ON
BUILD_SHARED_LIBS = ON

[cmake.defines.test=on]
BUILD_TESTS = ON

[cmake.defines.all(os=windows, test=on)]
BUILD_TESTS = OFF

[cmake.defines.test=off]
BUILD_TESTS = OFF

[dependencies]
folly-python
liboqs
libsodium
zlib-python
zstd-python

[dependencies.all(test=on, not(os=windows))]
googletest

[shipit.pathmap]
fbcode/fizz/public_tld = .
fbcode/fizz = fizz
